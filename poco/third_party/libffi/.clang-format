---
# Vendored upstream code — keep pristine so it can be diffed against and
# re-vendored from upstream. Do not reformat to the project style.
DisableFormat: true
# clang-format 4.0–12.0 uses the boolean form; 13.0+ accepts "Never".
# "false" is still parsed by newer versions as the legacy alias.
SortIncludes: false
